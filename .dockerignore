# Node modules (don't need to copy them since they will be installed in the image)
node_modules/

# Build output (we'll copy this later in the Dockerfile after building)
.next/

# Local environment variables (these should not be exposed in the image)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs and temporary files
npm-debug.log
yarn-debug.log
yarn-error.log
*.log

# Operating system files
.DS_Store
Thumbs.db

# Ignore the Dockerfile itself (if you don't want it to be copied into the image)
Dockerfile

# Ignore the .dockerignore itself (if you don't want it to be copied into the image)
.dockerignore

# Ignore IDE and editor-specific files
.vscode/
.idea/
*.swp
*.swo
